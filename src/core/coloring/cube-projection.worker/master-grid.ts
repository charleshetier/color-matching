import { RGB } from "core/model";

export const createMasterGrid = (cube: {size: number}) => (reference: RGB[]) => {
    // reference.reduce((master, referenceItem) => {
    //     // TODO build master grid
    // }, [])
};