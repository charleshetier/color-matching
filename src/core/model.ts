export interface UV {
    u: number,
    v: number
}

export type RGB = [number, number, number];
export type RGBIndices = [number, number, number];

export const RGB_RED_INDEX = 0;
export const RGB_GREEN_INDEX = 1;
export const RGB_BLUE_INDEX = 2;